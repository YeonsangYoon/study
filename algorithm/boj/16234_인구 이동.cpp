#include <bits/stdc++.h>
#define fastio ios::sync_with_stdio(0), cin.tie(0), cout.tie(0)
using namespace std;

int n, l, r;
int world[51][51];
int check[51][51];

bool move() {
	
}

void init() {
	cin >> n >> l >> r;
	for (int i = 1; i <= n; i++) {
		for (int j = 1; j <= n; j++) {
			cin >> world[i][j];
		}
	}
}

int main(void) {
	fastio;
	init();

	int day = 0;
	while (1) {

	}
}